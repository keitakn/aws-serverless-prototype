import * as mocha from "mocha";
import * as chai from "chai";

// TODO 動作確認の為だけの仮コミットなので後でちゃんと実装。 @keita-nishimoto
describe("Authentication", () => {
  it("testSuccess", () => {
    mocha.toString();

    chai.assert.equal(123, 123);
  });
});
